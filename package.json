{
  "name": "ego-chrome-extension",
  "version": "0.2.0",
  "description": "Local-first Chrome automation bridge and Codex skill with semantic snapshots.",
  "type": "module",
  "bin": {
    "ego-chrome": "./src/cli.mjs"
  },
  "scripts": {
    "bridge": "node ./src/bridge-process.mjs",
    "test": "node --test",
    "check": "node --check ./src/cli.mjs && node --check ./src/bridge.mjs && node --check ./src/runtime.mjs && node --check ./extension/service-worker.js && node --check ./extension/snapshot.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
